import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Kecepatan extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Kecepatan </Text>
      </View>
    );
  }
}

export default Kecepatan;
